# Synthetic sheep flock (SYNTHETIC DATA)

Generated by `abp simulate-sheep --seed 20260925` (ABP). `data/` holds the analysis inputs; `truth/` holds the simulated true breeding values and parameters, which analyses must never read (they are used only for simulation-based checks).
